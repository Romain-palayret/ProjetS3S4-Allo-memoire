#!/bin/bash

var=$(sed -n '3p' /usr/bin/SeReC/serec.config)
chemin=${var##* }
Ladate=$(date)
echo -e \\n$Ladate \\n >>$chemin/LogInstallation.log
apt -y --purge autoremove $1 2>/dev/null >>$chemin/LogInstallation.log


